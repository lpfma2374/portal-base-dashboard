name: Vercel Deploy (CI/CD)

# Template oficial de CI/CD Vercel com GitHub Actions.
# Secrets necessários (Settings > Secrets and variables > Actions):
#   VERCEL_TOKEN        - token de deploy (criado em vercel.com/account/tokens)
#   VERCEL_ORG_ID       - ID do team/conta (vercel.com/account/settings-teams)
#   VERCEL_PROJECT_ID   - ID do projeto (Settings > General do projeto no Vercel)
# Variáveis de ambiente do projeto (Vercel > Settings > Environment Variables):
#   CLOUDFLARE_ACCOUNT_ID, CLOUDFLARE_API_TOKEN, CLOUDFLARE_D1_DATABASE_ID

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
  VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

jobs:
  deploy-production:
    name: Deploy Production
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Vercel CLI
        run: npm install --global vercel@latest

      - name: Pull Vercel Environment Information
        run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}

      - name: Build Project Artifacts
        run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}

      - name: Deploy Project Artifacts to Vercel
        run: vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }}
